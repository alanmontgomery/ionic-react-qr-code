import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-qr-code',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
